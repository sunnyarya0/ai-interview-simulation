# AI Interview Simulator — frontend environment variables
# Copy to .env (or .env.local) to override the defaults baked into the services.

# Backend REST base URL
VITE_API_BASE_URL=http://localhost:8000

# Backend WebSocket base URL
VITE_WS_BASE_URL=ws://localhost:8000
